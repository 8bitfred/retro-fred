
To build and run from Linux:

  mkdir build
  cd build
  cmake ..
  cmake --build .
  ./cpp/fred

(note that the resolution and screen scale is currently set for
Android, which does not display as well in a Linux window)
